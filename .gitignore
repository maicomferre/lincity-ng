# Build Files
/build/
/build-*/

# Local development docs (not tracked, see .devdocs/README.md)
/.devdocs/

# Local agent entry-point (generated, source of truth is .devdocs/)
/AGENTS.md

# clangd/LSP database (symlink to build/compile_commands.json)
/compile_commands.json
